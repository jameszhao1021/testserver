/** Drop legacy playlist `version` fields from stored assignment snapshots. */
export function normalizePublishedPlaylistSnapshot(value) {
    const { version: _legacyVersion, ...rest } = value;
    return rest;
}
